# ===== CONFIGURACIÓN DEL SERVIDOR =====
# Entorno: development, production, test
NODE_ENV=development
# Puerto del servidor backend
PORT=3000

# ===== SEGURIDAD JWT =====
# CRÍTICO: Genera una clave segura con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# O ejecuta: .\generar-clave.ps1
# Debe ser única, secreta y de al menos 64 caracteres
JWT_SECRET=tu_clave_secreta_aqui_cambiar_obligatorio

# ===== CONFIGURACIÓN DE BASE DE DATOS =====
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=inventario_db
DB_PORT=3306

# ===== CONFIGURACIÓN DE BCRYPT =====
BCRYPT_ROUNDS=10

# ===== RATE LIMITING =====
# Ventana de tiempo en milisegundos (15 min = 900000)
RATE_LIMIT_WINDOW_MS=900000
# Máximo de intentos de login en la ventana
RATE_LIMIT_MAX_REQUESTS=5

# ===== CORS (OPCIONAL) =====
# Dominios permitidos separados por comas (opcional, se puede configurar en index.js)
# ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com

# ===== FRONTEND (OPCIONAL) =====
# URL del frontend para redirecciones
# FRONTEND_URL=https://tudominio.com
